meta {
  name: Logout
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/auth/logout
  body: none
  auth: none
}

tests {
  test("clears the session", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
