meta {
  name: Server status
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/status
  body: none
  auth: none
}

tests {
  test("responds", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
